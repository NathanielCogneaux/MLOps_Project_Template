import pandas as pd

### --- OMITTED --- ###

def lead_time_changer(lead_time: int) -> int:

    ### --- OMITTED --- ###

    return 0


def add_temp_features(df: pd.DataFrame, col: str) -> pd.DataFrame:

    ### --- OMITTED --- ###

    return df
